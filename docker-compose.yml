services:
  frontend:
    build:
      context: ./app-delivery-frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
    # depends_on:
    #   - backend

  # backend:
  #   build:
  #     context: ./backend
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./backend:/var/www
  #   depends_on:
  #     - database

  # database:
  #   image: mysql:5.7
  #   environment:
  #     MYSQL_DATABASE: laravel
  #     MYSQL_ROOT_PASSWORD: root
  #   ports:
  #     - "3306:3306"
